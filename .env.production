# BTDeck 前端生产环境配置
# 构建时会注入到前端代码中

# 后端 API 地址（使用相对路径，通过 nginx 代理）
# nginx 会将 /api/v1/ 请求代理到后端服务
VUE_APP_BASE_API=/api/v1

# WebSocket 地址（使用相对路径，通过 nginx 代理）
VUE_APP_WS_URL=/ws

# 应用标题
VUE_APP_TITLE=BitTorrent管理器

# 调试模式（生产环境建议关闭）
VUE_APP_DEBUG_MODE=false
